using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * This is a role of the class Villager. A chopper will gather wood and chop down trees as long as there is enough forest area and trees.
 * Wood is required for building new houses.
 * Feature will be implemented after the MVP.
 * @author Nicolas Durant
 */
public class Chopper : VillagerAlternate
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
