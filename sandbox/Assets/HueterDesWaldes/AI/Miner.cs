using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * This is a role of the class Villager. A miner is supposed to gather stone from higher terrain, which is used again for building houses.
 * Feature will be implemented after the MVP.
 * @author Nicolas Durant
 */
public class Miner : VillagerAlternate
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
